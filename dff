module dff(
    input clk,  
    input data,
    input rst, 
    output reg Q
   
);

    initial begin
        Q <= 0;
    end
    
    always @(posedge clk) begin
        if(rst)
            Q <= 1'b0;
        else
           Q <= data;
    end
        
endmodule
